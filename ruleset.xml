<?xml version="1.0"?>
<ruleset name="CustomStandard">
    <description>Custom coding standard.</description>

    <!-- Include everything from PSR-12 -->
    <rule ref="PSR12"/>

    <!-- Exclude specific sniffs -->
    <rule ref="PSR2.Methods.FunctionCallSignature.MultipleArguments">
        <exclude-pattern>*/app*</exclude-pattern>
    </rule>
</ruleset>
